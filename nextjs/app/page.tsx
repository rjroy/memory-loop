import { App } from "@/components/App";

/**
 * Root page for Memory Loop.
 *
 * Renders the full SPA-style application.
 * All routing is handled client-side via mode state in SessionContext.
 */
export default function Home() {
  return <App />;
}
