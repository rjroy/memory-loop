/**
 * Memory Loop - Main App Component
 *
 * Root component for the Memory Loop application.
 * Provides the app shell that will contain:
 * - Vault selection
 * - Note capture mode
 * - Discussion mode
 * - Tool transparency display
 */

import "./App.css";

export function App() {
  return (
    <div className="app">
      <header className="app-header">
        <h1 className="app-title">Memory Loop</h1>
      </header>
      <main className="app-main">
        {/* Content will be added in subsequent tasks */}
      </main>
    </div>
  );
}
